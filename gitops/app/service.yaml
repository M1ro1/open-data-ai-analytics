apiVersion: v1
kind: Service
metadata:
  name: web-app-service
  namespace: my-app-prod
spec:
  type: NodePort
  selector:
    app: web-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8501
      nodePort: 30081 # Порт для доступу ззовні